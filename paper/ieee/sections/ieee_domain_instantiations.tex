\section{Domain Instantiations under One Runtime Contract}
\label{sec:ieee-domains}

The key test of a universal safety layer is not whether every domain shares the same
dynamics or controller. It is whether the same runtime contract survives domain change
without losing semantic meaning. ORIUS evaluates that question across the defended
Battery, AV, and Healthcare rows.

\begin{table*}[t]
\centering
\caption{Defended ORIUS domain rows under a shared runtime contract.}
\label{tab:ieee-domain-rows}
\small
\begin{tabularx}{\textwidth}{@{}p{0.16\textwidth}p{0.20\textwidth}p{0.17\textwidth}p{0.16\textwidth}p{0.23\textwidth}@{}}
\toprule
\textbf{Domain} & \textbf{Degraded-observation hazard} & \textbf{Safety object} & \textbf{Current tier} & \textbf{Exact closure boundary}\\
\midrule
Battery energy storage & Dispatch legal on observed state but unsafe on true state of charge or reserve surface & State-of-charge and dispatch envelope preservation & Witness row & Deepest closure surface in the current stack.\\
Autonomous vehicles & Action proposed on stale or optimistic headway / collision state & TTC and entry-barrier preservation under repair & Defended bounded row & Bounded to the current TTC plus predictive-entry-barrier contract.\\
Healthcare monitoring & Clinical alerting or intervention released on degraded vital telemetry & Vital-alert envelope and bounded intervention discipline & Defended bounded row & Bounded to the current monitoring and intervention contract.\\
\bottomrule
\end{tabularx}
\end{table*}

\subsection{Autonomous vehicles}
The AV row is defended under a bounded safety object: time-to-collision and predictive
entry-barrier preservation when the perception or state-estimation surface degrades. The
value of the row is that ORIUS is not merely filtering a scalar dispatch command. It is
repairing an action inside a tightly constrained interaction envelope while preserving a
bounded fallback semantics. The row is defended, but still explicitly limited to the
current contract rather than advertised as full autonomy closure.


\subsection{Healthcare monitoring}
Healthcare matters because degraded observation can suppress intervention rather than
only create excessive actuation. ORIUS still fits that setting because the runtime layer
is defined over admissible decisions, fallback policies, and certificate semantics, not
only over physical force commands. The defended healthcare row is bounded to the current
monitoring and alerting contract, but it proves that the runtime grammar is not limited
to mechanical control loops.

\subsection{Future additional domains}
The active branch does not defend named additional domains beyond Battery, AV, and
Healthcare. Future extensions remain part of the architectural argument, but they are
not part of the current empirical claim surface until they clear the same replay,
runtime, governance, and bounded-prose obligations as the active three-domain lane.
