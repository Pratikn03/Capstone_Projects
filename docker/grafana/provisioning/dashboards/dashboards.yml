apiVersion: 1

providers:
  - name: 'GridPulse Dashboards'
    orgId: 1
    folder: 'GridPulse'
    folderUid: 'gridpulse'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
