# Example only. Copy to a local gitignored path and replace every value.
# Do not commit real ORIUS secrets.

certificate_keys:
  orius-cert-2026-01: "replace-with-strong-local-hmac-secret"
  orius-cert-2026-02: "replace-with-next-rotation-secret"

device_keys:
  edge-device-001:
    edge-key-2026-01: "replace-with-device-hmac-secret"

api_keys:
  local-admin-key:
    - "read"
    - "write"
    - "admin"
