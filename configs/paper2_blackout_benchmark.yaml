# Paper 2 blackout benchmark sweep config
# Claim: ch28, certificate half-life, expiration horizon, severe blackout benchmark

blackout_lengths: [1, 2, 4, 8, 16, 24, 48]  # 48h = catastrophic
initial_soc_pcts: [0.2, 0.4, 0.5, 0.6, 0.8]
sigma_d_regimes: [0.5, 1.0, 2.0, 3.0]

policies:
  - freeze_last_action
  - immediate_shutdown
  - dc3s_no_temporal
  - half_life_aware

constraints:
  soc_min_mwh: 10.0
  soc_max_mwh: 90.0
  time_step_hours: 1.0
  charge_efficiency: 0.95
  discharge_efficiency: 0.95

n_monte_carlo: 20
