objective:
  cost_weight: 1.0
  carbon_weight: 0.5

battery:
  capacity_mwh: 1000
  max_charge_mw: 200
  max_discharge_mw: 200
  efficiency: 0.92

grid:
  max_draw_mw: 10000
  price_usd_per_mwh: 70
  carbon_kg_per_mwh: 400

renewables:
  carbon_kg_per_mwh: 20
